# Скрипты VoluptAS

## 📋 Описание

Папка содержит служебные скрипты для управления проектом.

---

## 🚀 Основные скрипты

### bootstrap.ps1
**Назначение:** Автоматическая настройка окружения

**Использование:**
```powershell
.\scripts\bootstrap.ps1
```

**Что делает:**
- Находит Python (py -3.11, py -3, python)
- Создаёт виртуальное окружение `.venv`
- Устанавливает зависимости из `requirements.txt`
- Upgrade pip/setuptools/wheel

---

### verify.bat / verify.sh
**Назначение:** Verification loop (проверка кода)

**Использование:**
```bash
scripts\verify.bat       # Windows
scripts\verify.sh        # Linux/Mac
```

**Что проверяет:**
1. Lint (flake8)
2. Typecheck (mypy)
3. Tests (pytest)
4. Build (python -m py_compile)
5. Smoke test (импорты)

---

### restore_data.py
**Назначение:** Восстановление данных из backup CSV

**Использование:**
```bash
# Автоматически (последний backup)
python scripts\restore_data.py

# Вручную (конкретный файл)
python scripts\restore_data.py путь\к\файлу.csv
```

---

### check_portability.py
**Назначение:** Проверка переносимости проекта

**Использование:**
```bash
python scripts\check_portability.py
```

**Что проверяет:**
- Относительные пути
- Отсутствие хардкода
- Корректность credentials

---

### import_csv_full.py
**Назначение:** Полный импорт данных из CSV

**Использование:**
```python
from scripts.import_csv_full import import_from_csv

import_from_csv(csv_path, session)
```

**Вызывается из:**
- main.py (Файл → Импорт → CSV)
- restore_data.py

---

## 🔧 Вспомогательные

Эти скрипты используются внутренне:

- `bootstrap.ps1` — вызывается из `start_voluptas.bat`
- `verify.bat/sh` — вызывается вручную или в CI/CD
- `import_csv_full.py` — вызывается из main.py

---

## 📝 Добавление нового скрипта

1. Создать `.py` файл в `scripts/`
2. Добавить документацию в этот README
3. Обновить `.gitignore` если нужно
4. Протестировать на чистой системе

---

## ⚠️ Важно

- **НЕ удалять** `import_csv_full.py` — используется в main.py
- **НЕ удалять** `restore_data.py` — используется для backup
- **Можно удалять** только если скрипт не используется (помечено в CLEANUP_PLAN.md)

---

**Последнее обновление:** 2026-02-19

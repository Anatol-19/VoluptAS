# 🔄 GIT SYNC — Этап 0

**Статус:** IN PROGRESS  
**Дата:** 2026-02-20  
**Исполнитель:** GitHub Copilot  
**Приоритет:** 🔴 P0 (критично)

---

## 🎯 ПРОБЛЕМА

```
git status показывает:
- Local: 40 commits ahead
- Remote: 3 commits ahead
- Diverged!
```

**Решение:** `git pull --rebase` → resolve conflicts → `git push`

---

## 📋 ШАГИ

### Шаг 1: Pull с rebase

```bash
git pull --rebase origin main
```

**Что делает:**
- Забирает 3 remote коммита
- Применяет твои 40 коммитов поверх
- Сохраняет линейную историю

### Шаг 2: Разрешить конфликты (если есть)

**Если конфликты:**

1. Git покажет файлы с конфликтами
2. Открыть каждый файл
3. Выбрать изменения:
   ```
   <<<<<<< HEAD
   Твои изменения (локальные)
   =======
   Изменения с remote
   >>>>>>> origin/main
   ```
4. Сохранить нужную версию
5. `git add <файл>`
6. `git rebase --continue`

### Шаг 3: Push

```bash
git push origin main
```

---

## 🤖 ПРОМТ ДЛЯ COPILOT

```
Задача: Синхронизировать локальную и remote ветки

Контекст:
- Local: 40 commits ahead
- Remote: 3 commits ahead
- Diverged!

Команды:
1. git pull --rebase origin main
2. Если конфликты → открыть файлы, разрешить
3. git rebase --continue (после разрешения)
4. git push origin main

Файлы для проверки (могут быть конфликты):
- .ai/CONTINUITY.md
- .ai/AI_SYNC.md
- main.py
- src/integrations/qase.py
- src/utils/graph_builder.py

Важно:
- Не потерять локальные изменения!
- Сохранить все AI Framework файлы
- Сохранить Qase integration
- Сохранить Graph fixes

После push:
git status (должно быть "up to date")
```

---

## ✅ КРИТЕРИИ ГОТОВНОСТИ

- [ ] `git pull --rebase` завершён без ошибок
- [ ] Конфликты разрешены (если были)
- [ ] `git push origin main` успешен
- [ ] `git status` → "up to date"

---

**Начинай с команды:** `git pull --rebase origin main`

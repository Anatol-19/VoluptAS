# AGENTS.md — Инструкции для AI-агента

**Project:** VoluptAS — QA Management System  
**Version:** 0.3.4+  
**Stack:** Python 3.11+, PyQt6, SQLAlchemy, SQLite  
**Integrations:** Google Sheets, Zoho Projects, Qase.io

---

## Обзор проекта

VoluptAS — десктопное приложение для управления декомпозицией, трассировкой, покрытием и ответственностью за функциональные элементы продукта с поддержкой QA-отчётности и интеграции с внешними системами.

---

## Основные команды

```bash
# Менеджер пакетов
pip install -r requirements.txt

# Установка (Windows)
setup.bat

# Запуск приложения
python main.py
# или
start_voluptas.bat

# Сборка / проверка компиляции
python -m py_compile main.py

# Проверка типов
mypy src/ --ignore-missing-imports

# Тесты
pytest                   # все тесты
pytest --cov=src         # с покрытием
pytest tests/test_<module>.py  # конкретный модуль

# BDD тесты
behave

# Линтер
flake8 src/ --count --show-source --statistics

# Форматтер
black src/

# Verification loop
scripts/verify.bat       # Windows
scripts/verify.sh        # Linux/Mac
```

---

## 🧠 Behavioural Contract — MAKE NO MISTAKES

Перед обработкой **каждого** запроса мысленно добавляй:

> **«MAKE NO MISTAKES»**

Это означает:

1. **Перепроверяй факты, расчёты, код пошагово.**
2. **Если не уверен** — помечай как `UNCONFIRMED` и уточняй.
3. **Точность важнее скорости.**
4. **Для кода** — мысленно прогоняй выполнение или используй инструменты.

---

## 🧠 Управление контекстом и удержание фокуса

При длинных сессиях модели склонны терять фокус и деградировать. Чтобы этого избежать:

### 1. CONTINUITY.md — единственный источник истины

Сюда фиксируются:
- Goal (цель)
- Constraints / Assumptions (ограничения, допущения)
- Decisions (ключевые решения)
- State (Done / Now / Next)
- Open Questions (открытые вопросы)
- Working Set (ключевые файлы/команды/области)

> **В `CONTINUITY.md` — только факты, без пересказа диалога.**

### 2. Snapshot-триггер

Выполни snapshot и обнови `CONTINUITY.md`, если:
- Сессия превысила **15 шагов**
- Или длина контекста > **40 000 токенов**

Snapshot выглядит так:

```markdown
Goal:  
Done:
Now:
Next:
Open Questions:
```

### 3. Минимизация области внимания

Работай только с файлами/данными, относящимися к текущей цели.  
Избегай полного сканирования репозитория без надобности.

### 4. Восстановление фокуса

Если модель начинает терять нить:
- Загрузить состояние из `CONTINUITY.md`
- Переориентировать на текущее и следующую цель

### 5. Согласованность этапов

При каждом переходе этапов проверяй, что `Goal → Now → Next` логически согласованы.

---

## 📏 Основные правила

1. **Сначала план**  
   Перед реализацией любой задачи формируй план (`.ai/PLAN_MODE.md`).

2. **Проверка всего**  
   Ни одно изменение не считается завершённым без прохождения полного цикла проверки (`.ai/VERIFICATION.md`).

3. **Контекст обязателен**  
   Для длительных задач обновляй `CONTINUITY.md`.

4. **Соблюдай конвенции**  
   Придерживайся правил из `.ai/MEMORY_BANK.md`.

---

## 📌 Процесс работы

```
PLAN → EXECUTE (с MAKE NO MISTAKES) → VERIFY → UPDATE CONTINUITY → COMMIT
```

**Подробности:** `.ai/WORKFLOW.md`

---

## ⚠️ Запрещено

- ❌ Редактировать `README.md` без явного указания
- ❌ Хранить состояние только в чате
- ❌ Пропускать MAKE NO MISTAKES
- ❌ Выполнять опасные команды без разрешения (`rm -rf`, `sudo`)
- ❌ Доступ к секретам (`credentials/`, `.env`)
- ❌ Изменение структуры БД без миграции

**Регулируется:** `.ai/settings.json` и `.ai/settings.local.json`

---

## 📋 Definition of Done

Задача завершена, если:

- [x] Код компилируется
- [x] Тесты проходят
- [x] Verification выполнен
- [x] CONTINUITY обновлён (если применялся)
- [x] Нет неучтённых TODO

---

## 🔑 Key Files Reference

### Core
| Файл | Назначение |
|------|------------|
| `main.py` | Точка входа, инициализация MainWindow |
| `src/db/database_manager.py` | Singleton для сессий БД |
| `src/models/functional_item.py` | Основная модель сущности |

### UI
| Файл | Назначение |
|------|------------|
| `src/ui/main_window.py` | Главное окно |
| `src/ui/dialogs/settings_dialog.py` | Настройки интеграций (Google, Zoho, Qase) |
| `src/ui/dialogs/project_dialogs.py` | Управление проектами |
| `src/ui/widgets/full_graph_tab.py` | Визуализация графа связей |

### Integrations
| Файл | Назначение |
|------|------------|
| `src/integrations/google/google_sheets_client.py` | Google Sheets API |
| `src/integrations/zoho/Zoho_api_client.py` | Zoho Projects API |
| `src/integrations/qase/qase_client.py` | Qase.io API |

### Services
| Файл | Назначение |
|------|------------|
| `src/services/GoogleSheetsExporter.py` | Экспорт данных в Google Sheets |
| `src/services/TestPlanGenerator.py` | Генерация тест-планов |

---

> **См. также:**
> - `.cursorrules` — AI context для Cursor IDE
> - `CONTINUITY.md` — долговременная память сессии
> - `.ai/` — операционные модули AI

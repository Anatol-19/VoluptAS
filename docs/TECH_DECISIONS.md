# Технические решения — VoluptAS

**Версия:** 1.0  
**Дата:** 2026-02-19  
**Статус:** Утверждено

---

## 📋 Обзор

Этот документ фиксирует ключевые архитектурные решения принятые до начала разработки Инкремента 1.

---

## 1. Технические поля модели

### Контекст

В CSV-спецификации присутствуют поля:
- `maturity`
- `container`
- `database`
- `subsystems_involved`
- `external_services`

Этих полей нет в текущей модели `FunctionalItem`.

### Решение

**Принято:** Разделить поля на две категории

| Категория | Поля | Хранение | Фильтрация | MVP |
|-----------|------|----------|------------|-----|
| **Бизнес-критичные** | `maturity`, `status` | Отдельные колонки | ✅ Участвуют | ✅ |
| **Технический контекст** | `container`, `database`, `subsystems_involved`, `external_services` | `tech_context` JSON | ❌ Не фильтруется | ❌ (v0.5) |

### Обоснование

1. **maturity** — зрелость функционала (Idea/Beta/Stable/Deprecated)
   - Используется в отчётах
   - Может участвовать в фильтрах ("показать только Stable")
   - **Решение:** Отдельное поле в MVP

2. **status** — статус утверждения (Draft/Approved/Deprecated)
   - Критично для workflow
   - Валидация: нельзя сохранить isCrit=true без status=Approved
   - **Решение:** Отдельное поле в MVP

3. **Технические поля** (container, database, и т.д.)
   - Справочная информация
   - Не используется в фильтрах/GAP
   - **Решение:** `tech_context` JSON (отложено на v0.5)

### Реализация

```python
# Модель FunctionalItem
class FunctionalItem(Base):
    # ... существующие поля ...
    
    # MVP
    maturity = Column(String(50), default='Idea')  # Idea|Beta|Stable|Deprecated
    status = Column(String(50), default='Draft')   # Draft|Approved|Deprecated
    
    # v0.5 (опционально)
    tech_context = Column(JSON, default={})
    # {
    #   "container": "docker-container",
    #   "database": "PostgreSQL",
    #   "subsystems_involved": ["auth", "billing"],
    #   "external_services": ["Stripe", "AWS S3"]
    # }
```

### Миграция

```sql
-- Добавить поля
ALTER TABLE functional_items ADD COLUMN maturity VARCHAR(50) DEFAULT 'Idea';
ALTER TABLE functional_items ADD COLUMN status VARCHAR(50) DEFAULT 'Draft';
ALTER TABLE functional_items ADD COLUMN tech_context JSON;

-- Заполнить существующие записи
UPDATE functional_items SET status = 'Approved' WHERE created_at < '2026-01-01';
UPDATE functional_items SET status = 'Draft' WHERE created_at >= '2026-01-01';
```

---

## 2. Статусы и зрелость

### status (статус утверждения)

| Значение | Описание | Когда |
|----------|----------|-------|
| **Draft** | Черновик, не утверждено | Ручное создание |
| **Approved** | Утверждено, готово к работе | После ревью |
| **Deprecated** | Устарело, не используется | Вывод из эксплуатации |

**Правила:**
- При ручном создании: `status = Draft`
- При импорте из CSV: `status = Approved`
- При импорте из Google Sheets: `status = Approved`
- isCrit=true требует status=Approved (валидация)

---

### maturity (зрелость функционала)

| Значение | Описание | Пример |
|----------|----------|--------|
| **Idea** | Концепт, не реализовано | "Хотелка" в бэклоге |
| **Beta** | В разработке, частично работает | Спринт в процессе |
| **Stable** | Работает, протестировано | Релизнуто |
| **Deprecated** | Устарело, будет удалено | Legacy функционал |

**Правила:**
- При создании: `maturity = Idea`
- При начале разработки: `maturity = Beta`
- При релизе: `maturity = Stable`
- При выводе: `maturity = Deprecated`

---

## 3. Импорт CSV — стратегия MVP

### Проблема

§10 и §11 ТЗ противоречат:
- §10: "Импорт с маппингом колонок"
- §11: "Фиксированная схема CSV"

### Решение

**MVP (Инкремент 1):**
- CSV с **фиксированной схемой** (названия колонок строго определены)
- **Без UI маппинга**
- Ошибки логгируются, строки пропускаются

**v0.5 (Инкремент 2):**
- Мастер маппинга с UI
- Сохранение пресетов маппинга
- Валидация перед импортом

### Схема CSV (MVP)

```csv
FuncID,Title,Type,Module,Epic,Feature,Segment,ResponsibleQA,ResponsibleDev,IsCrit,IsFocus,Status,Maturity,TestCases,DocsLinks
MOD:FRONT,Frontend Module,Module,,,,,,Ivanov,Petrov,0,1,Approved,Stable,,
EPIC:AUTH,Authentication,Epic,FRONT,,,,,Ivanov,Petrov,1,1,Approved,Beta,TC-001,https://docs/auth
```

### Обработка ошибок

| Ошибка | Действие |
|--------|----------|
| Колонка не найдена | Лог: "Column X not found", пропуск строки |
| FuncID дубликат | Лог: "Duplicate FuncID: Y", пропуск строки |
| Ответственный не найден | Создать пользователя с status=Inactive |
| Неверный Type | Лог: "Invalid Type: Z", пропуск строки |

---

## 4. MetricsService

### Контекст

Метрики покрытия отображаются:
- В статусбаре таблицы
- В шапке матрицы покрытия
- В отчётах

Сейчас логика размазана по UI. Нужен единый сервис.

### Решение

**Создать:** `src/services/MetricsService.py`

**Интерфейс:**
```python
class MetricsService:
    def get(self, filters: dict) -> MetricsResult:
        """
        Args:
            filters: {type, segment, is_crit, responsible_qa, ...}
        
        Returns:
            MetricsResult {
                total: int,
                covered_tc: int,         # test_cases_linked не пуст
                covered_auto: int,       # automation_status = Automated
                covered_docs: int,       # documentation_links не пуст
                gaps_critical: int,      # is_crit=true без тестов
                gaps_total: int,
                coverage_tc_pct: float,  # covered_tc / total * 100
                coverage_auto_pct: float,
                coverage_docs_pct: float,
            }
        """
```

**Использование:**
```python
# В статусбаре таблицы
metrics = MetricsService().get(current_filters)
status_bar.setText(f"Всего: {metrics.total} | Покрытие ТК: {metrics.coverage_tc_pct}%")

# В шапке матрицы
metrics = MetricsService().get(matrix_filters)
header_label.setText(f"Критичные без тестов: {metrics.gaps_critical}")
```

---

## 5. Экраны (ТЗ v6)

### 5 экранов зафиксировано:

| № | Экран | Файл | Use Cases |
|---|-------|------|-----------|
| 1 | **Таблица** (с деревом слева) | `src/ui/widgets/main_tabs_widget.py` | UC-01, UC-02 |
| 2 | **Матрица покрытия** (drill-down) | `src/ui/widgets/coverage_matrix_tab.py` | UC-03 |
| 3 | **Граф связей** | `src/ui/widgets/full_graph_tab.py` | UC-01 |
| 4 | **Редактор сущности** | `src/ui/dialogs/item_editor.py` | UC-02, UC-04 |
| 5 | **Отчёты** | `src/ui/dialogs/report_generator_dialog.py` | UC-05 |

### Детализация

#### Экран 1: Таблица

**Компоненты:**
- Левая панель: дерево (Module → Epic → Feature → Story)
- Правая панель: таблица с фильтрами
- Статусбар: метрики от MetricsService

**Функции:**
- Клик по узлу дерева → фильтр таблицы
- Drag&drop веток → обновление parent_id
- Inline редактирование полей
- Фильтры: isCrit, isFocus, Responsible, Type, Segment

#### Экран 2: Матрица покрытия

**Режимы:**
- Flat (плоский список)
- Drill-down (раскрываемая иерархия)

**Колонки:**
- FuncID, Title, Type
- test_cases_linked
- automation_status
- documentation_links
- isCrit, isFocus
- Responsible QA/Dev

**Функции:**
- Срез "Без покрытия" (красная подсветка)
- Метрики в шапке (от MetricsService)
- Bulk редактирование (выбрать несколько → назначить QA)

#### Экран 3: Граф связей

**Визуализация:**
- Иерархия (parent-of)
- Функциональные связи (related-to)
- Цветовое кодирование по типу

**Функции:**
- Фильтр по глубине
- Клик по ноде → переход к элементу
- Экспорт в PNG

#### Экран 4: Редактор сущности

**Вкладки:**
1. Основные поля (FuncID, Title, Description, Type, Segment)
2. Иерархия (Module, Epic, Feature)
3. RACI (Responsible QA/Dev, Accountable, Consulted, Informed)
4. Покрытие (Test Cases, Automation, Docs)
5. Связи (parent, children, related)
6. BDD (Feature файл)
7. Инфраструктура (container, database, tech_context)

**Валидация:**
- Обязательные поля: FuncID, Title, Type
- isCrit=true → требует test_cases_linked
- status=Approved → требует Responsible QA/Dev

#### Экран 5: Отчёты

**Типы:**
- Sprint Report (по итогам спринта)
- GAP Report (дыры покрытия)
- RACI Report (нагрузка по ответственным)
- Coverage Report (общее покрытие)

**Форматы:**
- Markdown (для чата)
- PDF (для архива)
- Google Sheets (для команды)

---

## 6. Use Cases — статусы

| UC | Название | Статус | Приоритет |
|----|----------|--------|-----------|
| **UC-01** | Онбординг на новый проект | ✅ Принят | P0 |
| **UC-02** | Делегирование зоны и декомпозиция | ✅ Принят | P0 |
| **UC-03** | Матрица трассировки | ✅ Принят | P0 |
| **UC-04** | FuncID как универсальный маркер | ✅ Принят | P1 |
| **UC-05** | Синхронизация с Zoho | ✅ Принят | P1 (v0.5) |

**Файл:** `docs/ACCEPTANCE_CASES.html`

---

## 7. Открытые вопросы

| Вопрос | Решение | Статус |
|--------|---------|--------|
| BDD/автотесты результаты в отчёты | v next — отдельный инкремент | ⏸️ Отложено |
| Drag&drop в графе связей | MVP: только просмотр | ⏸️ Отложено |
| Undo/Redo | v0.5: команда + стек | ⏸️ Отложено |
| Алиасы сущностей | MVP: ручное заполнение | ✅ |

---

**Последнее обновление:** 2026-02-19  
**Следующее ревью:** После Спринта 1

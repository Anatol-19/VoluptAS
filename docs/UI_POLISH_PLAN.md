# UI Polish Plan — VoluptAS

**Дата:** 2026-02-19  
**Статус:** Исследование

---

## 🔍 Готовые UI решения для PyQt6

### 1. PyQt-Fluent-Widgets (Microsoft Fluent Design)

**Описание:** Библиотека реализует Fluent Design System (Windows 11 стиль).

**Возможности:**
- ✅ Кнопки с иконками
- ✅ Навигационное меню (аккордеон)
- ✅ Карточки (CardWidget)
- ✅ Выпадающие списки
- ✅ Тёмная тема из коробки
- ✅ Анимации

**Установка:**
```bash
pip install PyQt-Fluent-Widgets
```

**Пример:**
```python
from PyQtFluentWidgets import FluentWindow, FluentIcon, PushButton

class MyWindow(FluentWindow):
    def __init__(self):
        super().__init__()
        
        # Кнопка с иконкой
        btn = PushButton('Создать', icon=FluentIcon.ADD)
        
        # Навигация
        self.addSubInterface(self.home_interface, FluentIcon.HOME, 'Главная')
```

**Плюсы:**
- ✅ Современный вид (Windows 11)
- ✅ Много готовых виджетов
- ✅ Встроенная тёмная тема
- ✅ Хорошая документация

**Минусы:**
- ⚠️ Зависимость от Windows (некоторые виджеты)
- ⚠️ Больше весит (~5MB)

---

### 2. QDarkStyleSheet (Тёмная тема)

**Описание:** Тёмная тема для PyQt/PySide в стиле Qt Creator.

**Установка:**
```bash
pip install qdarkstyle
```

**Пример:**
```python
import qdarkstyle
app.setStyleSheet(qdarkstyle.load_stylesheet(qt_api='pyqt6'))
```

**Плюсы:**
- ✅ Работает на всех платформах
- ✅ Лёгкая (~100KB)
- ✅ Проверено временем

**Минусы:**
- ⚠️ Выглядит как Qt Creator (не супер-современно)

---

### 3. PyQt-Beautiful-Widgets

**Описание:** Красивые виджеты с современным дизайном.

**Возможности:**
- ✅ Переключатели (Toggle)
- ✅ Прогресс-бары
- ✅ Слайдеры
- ✅ Индикаторы

**Установка:**
```bash
pip install PyQtBeautifulWidgets
```

---

### 4. Custom CSS (Свои стили)

**Описание:** Стилизация через QSS (Qt Style Sheets).

**Пример:**
```python
self.button.setStyleSheet("""
    QPushButton {
        background: #1E90FF;
        color: white;
        border: none;
        border-radius: 6px;
        padding: 8px 16px;
        font-weight: bold;
    }
    QPushButton:hover {
        background: #1C86EE;
    }
    QPushButton:pressed {
        background: #1874CD;
    }
""")
```

**Плюсы:**
- ✅ Полный контроль
- ✅ Без зависимостей
- ✅ Лёгкое

**Минусы:**
- ⚠️ Нужно писать стили вручную
- ⚠️ Нет готовых компонентов

---

## 🎨 Рекомендация для VoluptAS

### Вариант A: PyQt-Fluent-Widgets + QDarkStyleSheet

**Комбинация:**
- PyQt-Fluent-Widgets для виджетов
- QDarkStyleSheet для общей темы

**Плюсы:**
- ✅ Современный вид
- ✅ Тёмная тема
- ✅ Много готовых компонентов

**Минусы:**
- ⚠️ 2 зависимости
- ⚠️ Больше весит

---

### Вариант B: Custom CSS (минимализм)

**Подход:**
- Свои стили для кнопок, меню, таблиц
- Без дополнительных зависимостей

**Плюсы:**
- ✅ Контроль над дизайном
- ✅ Нет зависимостей
- ✅ Лёгкое

**Минусы:**
- ⚠️ Время на разработку стилей

---

## 📋 План действий

### Этап 1: Быстрый старт (1 час)

**Установить PyQt-Fluent-Widgets:**
```bash
pip install PyQt-Fluent-Widgets
pip install qdarkstyle
```

**Применить тему:**
```python
# main.py
import qdarkstyle
from PyQtFluentWidgets import FluentWindow

app = QApplication(sys.argv)
app.setStyleSheet(qdarkstyle.load_stylesheet(qt_api='pyqt6'))

window = MainWindow()
window.show()
```

---

### Этап 2: Обновление кнопок (2 часа)

**Заменить:**
```python
# Было
btn = QPushButton('Создать')

# Стало
from PyQtFluentWidgets import PushButton, FluentIcon
btn = PushButton('Создать', icon=FluentIcon.ADD)
```

---

### Этап 3: Навигация (2 часа)

**Заменить меню на Fluent:**
```python
from PyQtFluentWidgets import NavigationItemPosition

self.addSubInterface(
    self.table_interface,
    FluentIcon.TABLE,
    'Таблица',
    position=NavigationItemPosition.SCROLL
)
```

---

## 🧪 Тестирование

**Чек-лист:**
- [ ] Кнопки выглядят современно
- [ ] Тёмная тема работает
- [ ] Меню навигации удобное
- [ ] Таблицы читаемы
- [ ] Диалоги красивые

---

## 📊 Оценка

| Этап | Часы | SP |
|------|------|----|
| Установка + тема | 1 | 1 |
| Кнопки | 2 | 2 |
| Навигация | 2 | 2 |
| Тестирование | 1 | 1 |
| **Всего** | **6** | **6 SP** |

---

**Решение:** Начинаем с **Вариант A** (PyQt-Fluent-Widgets + QDarkStyleSheet)

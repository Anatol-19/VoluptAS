# Developer Guide — VoluptAS

> **Единый источник истины для разработки**  
> Здесь: ТЗ, архитектура, стандарты, проблемы, ожидания

---

## 🎯 Назначение

Этот документ — **концентрат** для разработчика (AI или человека).

**Что здесь:**
- ✅ ТЗ и MVP (кратко)
- ✅ Архитектура (схема + визуальный документ)
- ✅ Стандарты кода
- ✅ Известные проблемы
- ✅ Ожидания от разработки

**Чего здесь нет:**
- ❌ Подробных инструкций (см. `docs/`)
- ❌ Истории изменений (см. `CHANGELOG.md`)
- ❌ Планов на будущее (см. `ROADMAP.md`)

---

## 🏛️ Архитектура (визуальный документ)

**Открыть в браузере:** [`docs/ARCHITECTURE_OF_REUSE.html`](ARCHITECTURE_OF_REUSE.html)

**Содержит:**
- Слоёная архитектура (Core → Self-hosted → SaaS → Open)
- Таблица владения функциями
- Поток данных (FuncID как связующий элемент)
- 3 открытых вопроса (TMS, Граф, Документы)
- AI notes для реализации

**Ключевой принцип:**
> VoluptAS — **оркестратор**, не монолит. Храним только карту функционала + связи. Всё остальное — внешние сервисы (Qase, Zoho, Docmost).

---

## 📋 ТЗ и MVP (кратко)

### Продукт

**VoluptAS** — десктопное приложение для управления декомпозицией, трассировкой, покрытием и ответственностью за функциональные элементы продукта.

### MVP (v0.4.0) — 10 критических функций

| № | Функция | Статус |
|---|---------|--------|
| 1 | CRUD элементов | ✅ |
| 2 | Иерархия Module→Epic→Feature→Story | ✅ |
| 3 | Таблица с фильтрами | ✅ |
| 4 | RACI-роли | ✅ |
| 5 | Флаги Crit/Focus | ✅ |
| 6 | CSV импорт/экспорт | ✅ |
| 7 | Google Sheets импорт/экспорт | ✅ |
| 8 | Multi-Project | ✅ |
| 9 | Справочники (редактирование) | ✅ |
| 10 | Zoho OAuth Wizard | ✅ |

**НЕ входит в MVP:**
- ❌ Граф связей (полноценный)
- ❌ Dashboard
- ❌ Удаление проектов
- ❌ Undo/Redo

### E2E тесты (8 сценариев)

1. Первый запуск (setup.bat)
2. Создание иерархии
3. RACI матрица
4. CSV импорт/экспорт
5. Google Sheets синхронизация
6. Multi-Project
7. Фильтры и поиск
8. Zoho OAuth

**Полные сценарии:** `docs/04_Technical_Spec/TZ.md`

---

## 🏗️ Архитектура

### Слоёная архитектура

```
┌─────────────────────────────────────────┐
│              UI (PyQt6)                 │
│  main_window.py, dialogs/, widgets/     │
├─────────────────────────────────────────┤
│           Services (Business)           │
│  GoogleSheetsExporter, TestPlanGen...   │
├─────────────────────────────────────────┤
│         Integrations (API clients)      │
│  Google, Zoho, Qase                     │
├─────────────────────────────────────────┤
│            Models (SQLAlchemy)          │
│  FunctionalItem, User, Relation...      │
├─────────────────────────────────────────┤
│          Database (SQLite)              │
│  DatabaseManager, session management    │
└─────────────────────────────────────────┘
```

### Multi-Project Architecture

```
data/
└── projects/
    └── default/
        ├── project.db        # БД проекта
        ├── config/           # Конфиги (projects.json, profiles.json)
        ├── bdd_features/     # BDD сценарии
        └── reports/          # Отчёты
```

**Принцип:** Каждый проект — отдельная БД, переключение без перезапуска.

### Ключевые файлы

| Файл | Назначение |
|------|------------|
| `main.py` | Точка входа, MainWindow |
| `src/db/database_manager.py` | Singleton для сессий БД |
| `src/models/functional_item.py` | Основная модель |
| `src/ui/main_window.py` | Главное окно |
| `src/ui/dialogs/settings_dialog.py` | Настройки интеграций |

---

## 📐 Стандарты кода

### Python

```python
# ✅ ПРАВИЛЬНО
def get_field_config_for_type(item_type: str) -> dict:
    """Возвращает конфигурацию полей для типа сущности."""
    configs = {...}
    return configs.get(item_type, configs['Feature'])

# ❌ НЕПРАВИЛЬНО
def get_field_config_for_type(item_type):  # нет type hints
    configs = {...}
    return configs.get(item_type, configs['Feature'])  # нет docstring
```

### Database

```python
# ✅ ПРАВИЛЬНО
session = DatabaseManager.get_session()
try:
    items = session.query(FunctionalItem).all()
    session.commit()
finally:
    session.close()

# ❌ НЕПРАВИЛЬНО
session = SessionLocal()  # Не создавать в UI-виджетах!
```

### UI (PyQt6)

```python
# ✅ ПРАВИЛЬНО
button.clicked.connect(self.on_button_clicked)

@pyqtSlot()
def on_button_clicked(self):
    # Обработка
    pass

# ❌ НЕПРАВИЛЬНО
# Прямой вызов методов без сигналов
```

### Code Style

- **PEP 8** — базовый стиль
- **Type hints** — для public API
- **Docstrings** — Google style
- **Комментарии** — русский для внутренних заметок
- **Форматтер** — `black src/`

---

## 🐛 Известные проблемы

### Критичные (P0)

| ID | Проблема | Статус | Решение |
|----|----------|--------|---------|
| **DB-01** | БД перемещена в `data/projects/default/` | 🟡 | Проверить загрузку |
| **CFG-01** | Конфиги перемещены | 🟡 | Проверить загрузку |
| **Zoho-01** | Нет поля ZOHO_AUTHORIZATION_CODE в UI | 🔴 | Добавить в settings_dialog.py |

### Некритичные (P1)

| ID | Проблема | Статус |
|----|----------|--------|
| **UX-01** | Нет удаления проектов | 🟡 Planned |
| **UX-02** | Нет undo/redo | 🟡 Planned |
| **UX-03** | Меню "Файл" → переименовать в "Проект" | 🟡 Planned |

### Технические долги

| ID | Долг | Приоритет |
|----|------|-----------|
| **TECH-01** | functional_id → FuncID (нейминг) | LOW |
| **TECH-02** | Удалить поле tags (дублирует aliases) | LOW |
| **TECH-03** | Ревизия знаков `──┐` и `───` | LOW |

---

## 🎯 Ожидания от разработки

### Перед задачей

1. Прочитать `.ai/AGENTS.md`
2. Проверить `.ai/CONTINUITY.md` (текущее состояние)
3. Создать план (`.ai/PLAN_MODE.md`)

### Во время задачи

1. Следовать стандартам (`.ai/MEMORY_BANK.md`)
2. Применять MAKE NO MISTAKES
3. Минимизировать область внимания

### После задачи

1. Пройти verification (`.ai/VERIFICATION.md`)
2. Обновить CONTINUITY.md
3. Закоммитить (`.ai/WORKFLOW.md`)

### Definition of Done

- [x] Код компилируется
- [x] Тесты проходят
- [x] Verification выполнен
- [x] CONTINUITY обновлён (если >1 шага)
- [x] Нет неучтённых TODO

---

## 📖 Документы (навигация)

### Для людей

| Документ | Где |
|----------|-----|
| README (описание проекта) | `README.md` |
| Быстрый старт | `docs/01_Getting_Started/` |
| Пользователь | `docs/02_User_Guide/` |
| ТЗ (полное) | `docs/04_Technical_Spec/TZ.md` |
| Планы | `docs/05_Roadmap/` |

### Для AI

| Документ | Где |
|----------|-----|
| AI-контракт | `.ai/AGENTS.md` |
| Планирование | `.ai/PLAN_MODE.md` |
| Проверка | `.ai/VERIFICATION.md` |
| Процесс | `.ai/WORKFLOW.md` |
| Конвенции | `.ai/MEMORY_BANK.md` |
| Память сессии | `.ai/CONTINUITY.md` |

### Технические

| Документ | Где |
|----------|-----|
| Структура проекта | `docs/PROJECT_STRUCTURE.md` |
| История чистки | `CLEANUP_PLAN.md` |
| Скрипты | `scripts/README.md` |

---

## 🚀 Команды

### Основные

```bash
# Установка
setup.bat

# Запуск
python main.py
# или
start_voluptas.bat

# Форматирование
black src/

# Проверка (если установлен black)
black src/ --check
```

### Интеграции

```bash
# Восстановление данных из backup
python scripts\restore_data.py

# Проверка переносимости
python scripts\check_portability.py
```

---

## 🆘 Поддержка

### Вопросы

1. **Где ТЗ?** → `docs/04_Technical_Spec/TZ.md`
2. **Где архитектура?** → Этот документ (см. выше)
3. **Где стандарты?** → `.ai/MEMORY_BANK.md`
4. **Где процессы?** → `.ai/WORKFLOW.md`

### Проблемы

1. **БД не загружается** → проверить путь `data/projects/default/project.db`
2. **Credentials не читаются** → проверить `credentials/`
3. **Конфиги не применяются** → проверить `data/projects/default/config/`

---

**Последнее обновление:** 2026-02-19  
**Версия:** 1.0

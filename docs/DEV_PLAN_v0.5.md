# VoluptAS — План разработки (v0.5)

**Дата:** 2026-02-19  
**Статус:** Активный

---

## 🔴 КРИТИЧНО — Исправлено в MVP

| Defect | Описание | Статус |
|--------|----------|--------|
| **#1** | Starter Wizard — шаблоны вместо динамической помощи | ✅ FIXED (частично) |
| **#2** | Нет inline редактирования | ✅ FIXED |
| **#3** | Выпадашки не обновляются | ✅ FIXED |
| **#4** | FuncID не генерируется | ✅ FIXED |
| **#5** | Нет создания дочерних элементов | ✅ FIXED |
| **#6** | Dropdowns показывают историю, не сущности | ✅ FIXED |

---

## 🟡 REQUIRES RETHINK — Отложено на v0.5

### 1. Starter Wizard — Динамическая помощь

**Проблема:** Текущая реализация — просто шаблоны.

**Ожидание:**
- Динамическая подсказка **в процессе** декомпозиции
- Контекстная помощь: "Декомпозировать Epic" → подсказка Feature
- Интеллектуальные подсказки по сегментам
- Создание родителя "на лету" если нет

**План:**
```
v0.5: Dynamic Decomposition Assistant
- Боковая панель "Помощник" (открывается с Ctrl+Shift+A)
- При выделении элемента → показывает рекомендации
- "Похожие элементы" — ML или правила
- "Что дальше?" — шаблоны декомпозиции
```

---

### 2. Граф связей — ✅ ИСПРАВЛЕНО в MVP

**Проблема:** Граф не отображал связи "на лету" из атрибутов.

**Решение (v0.4):**
- ✅ `src/utils/graph_builder.py` — построение графа из атрибутов
- ✅ Связи из `parent_id`, `module`, `epic`, `feature`
- ✅ NetworkX + Matplotlib визуализация
- ✅ Цветовое кодирование по типу
- ✅ Стрелки для связей

**Текущее состояние:**
- ✅ Full Graph Tab — показывает всю иерархию
- ✅ Mini Graph Widget — показывает соседей (родители + дети)
- ✅ Цвета: Module=синий, Epic=зелёный, Feature=оранжевый
- ✅ Стрелки: белые для parent-of

**План v0.5 (опционально):**
- Исследовать: Vis.js для интерактивности
- PyQtWebEngine для веб-визуализации
- Drag-and-drop для изменения parent

---

## 🟢 v0.5 — Новые функции

### 3. Sandbox Protection

**Проблема:** Sandbox — учебный проект, должен сбрасываться.

**Требования:**
- Нельзя удалить Sandbox проект
- Сброс к исходному состоянию (кнопка "Reset Sandbox")
- Запрет на изменение (read-only mode)
- Авто-сброс при закрытии (опционально)

**План:**
```
v0.5: Sandbox Protection
- Флаг project.is_sandbox = True
- UI: замок на проекте (нельзя удалить)
- Кнопка "🔄 Reset Sandbox" → очистка + шаблоны
- Опция: "Автосброс при выходе"
- Шаблон по умолчанию: VoluptAS Documentation
```

**Реализация:**
```python
# projects.json
{
  "sandbox": {
    "id": "sandbox",
    "name": "Sandbox",
    "is_sandbox": true,
    "auto_reset_on_exit": false,
    "template": "voluptas_docs"
  }
}

# main.py
def delete_project(self, project_id):
    project = self.project_manager.get_project(project_id)
    if project.is_sandbox:
        QMessageBox.warning(self, 'Защита Sandbox', 
            '❌ Нельзя удалить Sandbox проект!')
        return
```

---

### 4. Safe Delete

**Проблема:** Удаление элементов без проверки связей.

**Требования:**
- Проверка дочерних элементов перед удалением
- Проверка связей (related-to, parent-of)
- "Мягкое удаление" (flag is_deleted)
- Корзина (восстановление в течение 30 дней)
- Полное стирание (hard delete) с подтверждением

**План:**
```
v0.5: Safe Delete System
- Проверка: есть ли дочерние? → предупредить
- Проверка: есть ли связи? → показать список
- Soft delete: is_deleted = True, deleted_at
- Trash bin: список удалённых, restore/delete
- Hard delete: окончательное удаление (нельзя отменить)
```

**Диалог удаления:**
```
┌─ Удаление: EPIC:FRONT.AUTH ─────────────────┐
│                                              │
│ ⚠️  Найдены дочерние элементы:              │
│   • FEAT:FRONT.AUTH.LOGIN (Feature)         │
│   • FEAT:FRONT.AUTH.LOGOUT (Feature)        │
│                                              │
│ ⚠️  Найдены связи:                          │
│   • STORY:FRONT.AUTH.LOGIN.SOCIAL           │
│     (related-to: FEAT:FRONT.AUTH.LOGIN)     │
│                                              │
│ [ ] Удалить дочерние (каскад)               │
│ [ ] Мягкое удаление (в корзину)             │
│ [ ] Полное удаление (безвозвратно)          │
│                                              │
│            [Отмена]  [Удалить]               │
└──────────────────────────────────────────────┘
```

**Модель:**
```python
class FunctionalItem(Base):
    # ...
    is_deleted = Column(Boolean, default=False)
    deleted_at = Column(DateTime, nullable=True)
    deleted_by = Column(Integer, ForeignKey('users.id'))
    
    # Query helper
    @classmethod
    def active(cls):
        return session.query(cls).filter_by(is_deleted=False)
```

---

### 5. XMind-подобный Wizard

**Проблема:** Нет визуального редактора декомпозиции.

**Требования:**
- Drag-and-drop ноды
- Быстрое создание (двойной клик → ввести Title)
- Редактирование свойств в боковой панели
- Сохранение в базу с связями

**План:**
```
v0.5: Visual Decomposition Editor
- Интеграция: MindMap библиотека (PyQt или веб)
- Холст: ноды и связи
- Боковая панель: свойства выделенной ноды
- Кнопка "Сохранить в базу"
- Экспорт: .mm (FreeMind), .xmind
```

---

## 📋 Спринт 1 — Завершён

**User Stories:**
- [x] US-001: Импорт CSV (исправлен баг)
- [x] US-002: Экспорт Google Sheets (готов)
- [x] US-003: Inline редактирование
- [x] US-004: Создание дочерних элементов
- [x] US-005: FuncID авто-генерация

**Всего:** 5/5 ✅

---

## 📋 Спринт 2 — Планирование (v0.5)

**Кандидаты:**

| Задача | SP | Приоритет |
|--------|----|-----------|
| UI Polish (PyQt-Fluent-Widgets) | 3 | 🔴 HIGH |
| Sandbox Protection | 3 | 🟡 MEDIUM |
| Safe Delete | 5 | 🔴 HIGH |
| Dynamic Decomposition Assistant | 8 | 🟢 LOW |
| Visual Decomposition Editor (XMind) | 13 | 🟢 LOW (v0.6) |
| Graph Interactivity (Vis.js) | 8 | 🟢 LOW (v0.6) |

---

## 📊 Технический долг

| Долг | Описание | Приоритет |
|------|----------|-----------|
| **Widget consistency** | Одинаковые виджеты inline vs диалог | 🟡 MEDIUM |
| **Segment management** | Отдельное меню для справочника сегментов | 🟢 LOW |
| **Tests** | Unit-тесты для graph_builder | 🟡 MEDIUM |
| **Docs** | Обновить README с новым функционалом | 🟢 LOW |

---

**Последнее обновление:** 2026-02-19  
**Следующее планирование:** После тестирования v0.4

# Copilot Setup — VoluptAS

**Дата:** 2026-02-19

---

## 📋 Настройка GitHub Copilot

### 1. Установить инструкции

Файл: `.github/copilot-instructions.md`

Этот файл автоматически используется GitHub Copilot для понимания контекста проекта.

### 2. Как работает

Copilot читает:
- `.github/copilot-instructions.md` — основной контекст
- Текущий файл над которым вы работаете
- Открытые файлы в редакторе

### 3. Рекомендации

**Для лучших результатов:**

1. **Откройте relevant файлы:**
   - При работе с графом: `src/utils/graph_builder.py`, `src/ui/widgets/full_graph_tab.py`
   - При работе с UI: `main.py`, `src/ui/dialogs/`

2. **Используйте комментарии:**
   ```python
   # TODO: Исправить поиск родителей для графа
   # Copilot предложит решение на основе контекста
   ```

3. **Задавайте вопросы в комментариях:**
   ```python
   # Как найти родителя по module атрибуту?
   # Copilot предложит find_parent_by_title()
   ```

---

## 🤖 Copilot vs Qwen Code

| Функция | GitHub Copilot | Qwen Code |
|---------|----------------|-----------|
| **Контекст** | `.github/copilot-instructions.md` | `.qwen/` директория |
| **Режим** | Автодополнение | Чат + код |
| **Интеграции** | VS Code, JetBrains | VS Code |
| **Язык** | Английский предпочтительно | Русский + Английский |

**Рекомендация:** Использовать оба инструмента:
- **Copilot** — для автодополнения кода
- **Qwen Code** — для сложных задач и рефакторинга

---

## 📝 Обновление инструкций

При добавлении нового функционала:

1. Обновить `.github/copilot-instructions.md`:
   - Добавить новые модели
   - Обновить архитектуру
   - Добавить примеры кода

2. Обновить `.qwen/` (для Qwen Code):
   - Аналогично Copilot

---

## 🧪 Примеры использования

### Пример 1: Генерация кода

**Запрос:**
```python
# Создать функцию для экспорта графа в PNG
```

**Copilot предложит:**
```python
def export_graph_to_png(graph, filepath):
    """Экспорт графа в PNG файл"""
    figure = Figure(figsize=(14, 8), facecolor="#1e1e1e")
    canvas = FigureCanvas(figure)
    figure.savefig(filepath, dpi=300, bbox_inches="tight", facecolor="#1e1e1e")
```

### Пример 2: Рефакторинг

**Запрос:**
```python
# Рефакторинг: вынести поиск родителей в отдельную функцию
```

**Copilot предложит:**
```python
def find_parent_by_title(items, title, type_filter):
    """Поиск родителя по названию и типу"""
    for item in items:
        if item.type == type_filter and item.title.lower() == title.lower():
            return item
    return None
```

---

## 🔧 Troubleshooting

### Copilot не предлагает код

**Решение:**
1. Проверить что `.github/copilot-instructions.md` существует
2. Перезагрузить VS Code
3. Проверить подписку Copilot

### Copilot предлагает устаревший код

**Решение:**
1. Обновить `.github/copilot-instructions.md`
2. Добавить комментарии с контекстом
3. Использовать `# NEW:` префикс для нового кода

---

**Последнее обновление:** 2026-02-19
